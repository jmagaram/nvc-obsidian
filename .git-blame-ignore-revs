# Commits that only reformatted code. `git blame` should walk past these to
# whatever commit last said something about the line.
#
# GitHub reads this file on its own. Locally it needs saying once per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add a commit here only if it is formatting and nothing else.

# Run Prettier over the repo
33e8fab04328ffdfd91b243caeb5dfa5a265360d
