# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# The plugin build. `build/` because that is one of the places the community
# directory's scanner looks for main.js — see vite.plugin.config.ts.
build

# Covers .env.local, which names the vault to deploy into — one machine's path.
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
